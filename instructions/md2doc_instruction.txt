在本项目维护或编写 Markdown 文件时，请遵循以下规则：
- 章节分页：使用标准分割线 --- 作为换页符。
- Mermaid图表：使用 ```{.mermaid #fig:唯一ID caption="图表描述"} 格式。
- 独立图片：使用 ![图图描述](路径){#fig:图片ID} 格式，大括号前不加空格。
- 手动序号：在文档标题、段落、列表前手动写好类似 1.1、1.1.1 的多级序号，禁止省略交由工具动态生成。
- 转换兼容限制：禁止使用任何 HTML 标签，如 <br>、<font> 等；图片使用相对路径；只使用标准标题和管道表格。
