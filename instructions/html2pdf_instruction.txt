在维护或编写 HTML 转单页 PDF (html2pdf) 源文件时，请遵循以下规则：
- 自适应页面大小：在 CSS 中使用 @page 规则，或使用 px 指定容器的绝对宽高度，以便 Chromium 自动适配单页 PDF 大小。
- 渲染延迟：如果页面包含复杂的 JavaScript 动态绘制（如 ECharts、D3），请在项目设置中配置合适的渲染等待延迟。
- 转换兼容限制：避免使用外部不可访问的网络资源；尽量使用内联或本地 CSS 样式；确保没有会导致 Chromium 渲染卡死的无限循环 JavaScript。
